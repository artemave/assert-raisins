{
  "name": "donc",
  "version": "1.3.0",
  "description": "Minimalistic test runner that does NOT dynamically require test files. And so `donc --inspect-brk` will have all project files loaded.",
  "main": "index.js",
  "bin": "bin/donc",
  "author": "github.com/artemave",
  "license": "MIT",
  "repository": "github:artemave/donc",
  "scripts": {
    "test": "./run_tests"
  },
  "dependencies": {
    "glob": "^7.1.6"
  },
  "devDependencies": {}
}
