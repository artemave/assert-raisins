{
  "name": "assert-raisins",
  "type": "module",
  "version": "1.4.0",
  "description": "Minimalistic test runner that does NOT dynamically require test files. And so `ars --inspect-brk` will have all project files loaded.",
  "main": "index.js",
  "bin": "bin/ars",
  "author": "github.com/artemave",
  "license": "MIT",
  "repository": "github:artemave/assert-raisins",
  "scripts": {
    "test": "node --test --enable-source-maps --loader ts-node/esm lib/*.test.ts && node --test --test-concurrency=1 lib/*.test.js index.test.js"
  },
  "dependencies": {
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "latest",
    "eslint": "^8.57.0",
    "ts-node": "latest",
    "typescript": "latest",
    "typescript-eslint": "^7.1.0"
  }
}
